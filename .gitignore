# dependencies (monorepo: puede haber node_modules por workspace, no solo en la raíz)
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/

# production
build/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# cliente Prisma generado por app (ver output en cada schema.prisma) — build artifact
# ** al inicio: sin esto, un patrón con "/" en medio queda anclado a la raíz del repo
# y no ignora apps/<app>/src/generated/ (solo <root>/src/generated/).
**/src/generated/

.vercel
